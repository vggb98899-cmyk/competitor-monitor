# AI协作开发规范 v1.0

## 一、项目结构（四层强制分离）
- config.py：URL、超时、重试、路径、选择器
- utils.py：通用工具（存Excel、日志、字符串处理）
- crawler.py / parser.py：业务逻辑
- main.py：流程编排，不超过50行

## 二、每次生成代码前必须回答的问题
1. 这个模块属于四层中的哪一层？
2. 依赖哪些其他模块？
3. 如果页面改版或数据为空，代码怎么应对？
4. 执行时间和资源消耗大概多少？
5. 如果这个模块失败，会不会导致整个流程崩溃？

## 三、生成代码时必须包含
1. 函数/类的docstring
2. 关键步骤的中文注释
3. 异常捕获和日志记录
4. 配置项从config.py读取

## 四、生成完成后必须输出
1. 技术决策日志
2. 修改建议
3. 潜在风险
