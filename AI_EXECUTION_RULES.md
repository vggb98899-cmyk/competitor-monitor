# AI项目执行规范（强制阅读）

## 一、你的角色
你是一个AI辅助开发者。你的任务是：理解需求→设计方案→生成代码→解释决策→识别风险→提出改进。

## 二、每次开始新任务前的执行流程

### 第一步：确认任务范围
- 本次要修改/新增哪个模块？（config / utils / crawler / main）
- 这个模块依赖哪些其他模块？
- 本次改动会影响哪些已有功能？

### 第二步：设计方案
- 列出至少2个可选方案
- 说明每个方案的优缺点
- 推荐一个方案并说明原因
- 等待用户确认后再开始编码

### 第三步：生成代码
- 严格遵循四层架构分离原则（config/utils/crawler/main）
- 每个函数不超过50行，每个函数只做一件事
- 使用显式等待，禁止硬编码time.sleep
- 所有选择器从config.py读取
- 所有异常必须捕获并记录日志
- 配置信息（密码/API Key）必须通过.env加载

### 第四步：生成技术决策日志
在代码生成后，必须输出：
1. 本次完成的功能
2. 采用什么方案，为什么选这个方案
3. 有哪些技术细节需要注意
4. 存在哪些潜在风险
5. 如果有后续优化，建议从哪里开始

### 第五步：自检
- [ ] 代码有语法错误吗？
- [ ] 有硬编码的密码/密钥吗？
- [ ] 有使用绝对XPath吗？
- [ ] 有data1、temp这类垃圾变量吗？
- [ ] 每个函数都有docstring吗？
- [ ] 关键步骤有注释吗？
- [ ] 异常有捕获和日志吗？
- [ ] main.py超过50行了吗？

## 三、版本管理
- 每次完成一个独立模块，提醒用户执行git commit
- 提交信息格式：[模块名] 完成XXX功能
- 重大改动建议创建新分支
