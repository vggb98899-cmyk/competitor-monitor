# 知识点笔记（随时翻阅）

## 1. 什么是 products.json？

Shopify 店铺除了给普通用户看的网页，还开了一个"后门"专门给程序员取数据。

| 对比 | 普通网页 | JSON接口 |
| :--- | :--- | :--- |
| 地址 | `/collections/all` | `/collections/all/products.json` |
| 内容 | HTML + 样式 + 图片 + 导航栏 | **纯数据**（标题、价格等） |
| 谁用 | 普通顾客 | 程序员/爬虫 |

**所有 Shopify 店铺都有这个接口**，不是漏洞，是Shopify自带的功能。

---

## 2. 四层架构（每个爬虫项目的标准结构）

```
my_project/
├── config.py    → 配置：网址、关键词、超时时间、选择器
├── utils.py     → 工具：存Excel、写日志、通用函数
├── crawler.py   → 爬虫：实际去网上拿数据的代码
├── main.py      → 入口：编排流程，控制在50行以内
```

**为什么这样分？**
- 改配置不用翻代码 → 改 `config.py` 就行
- 换存储方式不用改爬虫逻辑 → 改 `utils.py` 就行
- 每个文件各管各的事，出了bug好找

---

## 3. 为什么不用 time.sleep()？

`time.sleep(3)` 的意思是"不管三七二十一，先等3秒"。

但万一网络慢，3秒不够 → 报错
万一网络快，0.5秒就够了 → 白等2.5秒

**正确做法：显式等待**
- 等某个元素出现 → 最长等10秒，提前出现就提前继续
- 比硬等更智能、更稳定

---

## 4. 选择器为什么要放 config.py？

假设今天Manduka改版了，商品标题的CSS类名从 `.product-title` 变成了 `.item-name`：

**选择器放config.py →** 只改一行配置
**选择器写在代码里 →** 满代码翻找，改漏一个就报错

---

## 5. .env 文件是干什么的？

存密码、API密钥、敏感信息。不提交到Git，防止泄露。

```
# .env 文件内容（不上传到Git）
API_KEY=sk-xxxxx
DATABASE_PASSWORD=123456
```

代码里通过 `os.getenv("API_KEY")` 读取，密钥不会写在代码里。
