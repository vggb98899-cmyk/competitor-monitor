# 钉钉Excel文件下载失败解决

## 一、优先处理【代理：可疑】（最主要原因）
钉钉PC端会跟随系统代理，代理异常会直接拦截文件下载

1. 按下快捷键 `Win+I` 打开**Windows设置** → 网络和Internet → **代理**
2. 把下面这几项全部关闭：
   - 自动检测设置：关闭
   - 使用设置脚本：关闭
   - 手动设置代理：关闭（如有地址端口全部清空）
3. 关掉VPN、梯子、各类网络加速工具，重启钉钉再尝试下载文件。

## 二、修复【DNS：建议自查】

### 方案1：一键改公共DNS（推荐）
1. 打开电脑「控制面板→网络和共享中心」，点开当前连接的WiFi/以太网
2. IPv4属性，手动设置DNS：
   - 首选：`223.5.5.5`
   - 备选：`223.6.6.6`（阿里公共DNS，适配钉钉）
   - 也可以用 `114.114.114.114` / `8.8.8.8`
3. 按下 `Win+R` 输入 `cmd`，执行命令刷新DNS缓存：
   ```
   ipconfig /flushdns
   ```

## 三、钉钉端简易应急办法（不用改配置）
1. 直接右键聊天里的Excel文件 → **在浏览器打开**，用浏览器下载，绕开钉钉客户端下载链路
2. 退出钉钉账号彻底重登，或者直接重启电脑
3. 打开钉钉诊断工具右上角**开始检测**，让工具自动修复异常项

## 四、兜底排查
1. 检查C盘剩余空间，磁盘满了会无法写入下载文件
2. 临时关闭电脑管家、防火墙、杀毒软件，这类软件容易拦截钉钉下载进程
3. 若为公司内网，需要联系网管确认企业代理/内网策略是否封禁钉钉文件下载
