# 需求文档 v0.1（待确认）

## 1. 项目背景
采集海外电商商品数据，用于价格监控和竞品分析。第一期先跑通最小闭环，后续迭代。

## 2. 采集范围

| 项目 | 内容 |
| :--- | :--- |
| **目标平台** | Shopify 店铺（具体店铺 URL 待确认） |
| **测试关键词** | "yoga mat" |
| **采集字段** | 商品标题、当前价格、月销量 |
| **数据量预期** | 跑通即可，初期无数量要求 |

## 3. 输出格式
- 文件格式：Excel（.xlsx）
- 运行方式：执行 `python main.py`，自动生成 Excel
- 输出位置：项目根目录下 `output/` 文件夹

## 4. 时间要求
- 当天出一版能跑的 1.0 版本
- 哪怕数据不全，先看结果再迭代

## 5. 验收标准
- [ ] 脚本能正常启动、执行完毕、不报错退出
- [ ] 输出一个格式正确的 Excel 文件
- [ ] Excel 中包含标题、价格两列有数据
- [ ] 数据来自目标店铺的真实商品

## 6. ⚠️ 待确认事项

### 问题 A：具体目标店铺是哪家？
你说"一家 Shopify 店铺"，能给我一个具体的店铺 URL 吗？
例：`https://xxx.myshopify.com/` 或 `https://www.somebrand.com/`（只要是 Shopify 搭建的就行）
> ⚡ 这个不确定，我没办法写搜索逻辑。

### 问题 B：「月销量」字段在 Shopify 上可能拿不到
Shopify 是独立站建站工具，**大多数店铺的商品详情页不显示销量数字**。这与 Amazon 不同。
- 如果店铺有销量显示 → 可以直接抓取
- 如果店铺没有显示 → 这个字段会为空，是否需要：
  - **方案 a**：先空着，后续再想办法
  - **方案 b**：替换为"评分"或"评论数"等可获取字段
  - **方案 c**：你确认一下目标店铺确实显示销量

---

**请确认以上两个问题，确认后我立即出技术方案并开始编码。**
