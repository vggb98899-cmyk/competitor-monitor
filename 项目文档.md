# 竞品数据分析系统 —— 企业级项目文档

## 一、项目概述

### 1.1 项目背景
跨境电商卖家需要对标竞品价格、监控新品动态、分析品类趋势，以支撑选品决策和定价策略。传统方式依赖人工每日手动复制粘贴，效率低、易遗漏。

### 1.2 项目目标
搭建一套全自动的跨平台竞品数据采集+分析+告警系统，覆盖8家Shopify店铺和1家eBay店铺，支持日报推送、异常告警、周报分析和自助查询看板。

### 1.3 技术栈
| 工具 | 用途 | 选型理由 |
|------|------|----------|
| Python 3.14 | 开发语言 | 生态丰富，爬虫/AI/数据分析库齐全 |
| curl_cffi | HTTP请求库 | 支持TLS指纹模拟，绕过WAF检测 |
| Playwright | 浏览器自动化 | 绕过eBay安全验证，模拟真人操作 |
| MySQL 8.0 | 数据存储 | 支持历史数据回溯和SQL分析 |
| Streamlit | 数据看板 | 零前端基础快速搭建交互式页面 |
| DeepSeek API | AI分析 | 自然语言生成周报，自动分析数据 |
| 飞书Webhook | 消息推送 | 免费，支持群消息通知 |

---

## 二、系统架构

### 2.1 整体架构图
```
采集层                         存储层               展示层
┌─────────────┐              ┌────────┐         ┌──────────┐
│ Shopify 6家  │──curl_cffi──→│        │──SQL──→│ Streamlit│
│              │              │ MySQL  │         │  看板     │
│ eBay 1家     │──Playwright─→│        │         └──────────┘
│              │              └────────┘              │
│ 待接入(Etsy) │                  │                   │
└─────────────┘                  │                   │
       │                         │                   │
       ▼                         ▼                   ▼
┌─────────────┐         ┌────────────────┐   ┌────────────────┐
│ 每日main.py  │──定时──→│ price_history  │   │ 飞书日报/周报   │
│ 周一+周报     │         │ discovery_history│   │ + 告警推送      │
└─────────────┘         └────────────────┘   └────────────────┘
```

### 2.2 数据库设计
```
products 表（商品主信息）
├── id (主键)
├── store (店铺名)
├── title (商品标题)
├── category (品类)
├── handle (Shopify handle)
├── first_seen (首次发现)
├── last_seen (最后出现)
└── is_active (是否在售)

snapshots 表（快照记录，每天一条）
├── id (主键)
├── product_id (关联商品)
├── price (当前价格)
├── rating (评分)
├── status (在售/售罄)
└── captured_at (采集日期)

run_logs 表（运行日志）
├── id (主键)
├── run_date (运行日期)
├── stores_ok (成功店铺数)
├── stores_fail (失败店铺数)
├── total_products (商品总数)
├── alerts_count (告警数)
└── errors (错误信息)
```

**为什么分两张表？**
- `products` 存商品固定信息，每条商品只有一条记录
- `snapshots` 存每天的价格/评分变化，支持历史回溯
- 想查"过去30天某品类均价变化" → 一条SQL就搞定

---

## 三、核心功能模块

### 3.1 数据采集
| 平台 | 技术方案 | 数据量 |
|------|----------|--------|
| Shopify 8家 | curl_cffi 调用 `/collections/all/products.json` | 约1600个商品 |
| eBay 1家 | Playwright + 系统Chrome，绕过安全验证 | 约52个商品 |

### 3.2 品类映射
当Shopify未提供 `product_type` 时，通过标题关键词自动匹配品类。例如：
- 标题含 "tent" → Tent
- 标题含 "power station" → Portable Power
- 标题含 "jacket" → Apparel

### 3.3 告警系统
- **价格变动>5%** → 推送飞书告警
- **新品上架** → 自动判断重要性（价格≥$100或含Pro/GTX等标记为"重要"）
- **商品下架** → 推送上架/下架变化
- eBay店铺**不触发告警**，只在周报中提及

### 3.4 AI周报
每周一自动调用DeepSeek API，分析过去7天数据，生成：
1. 价格变动最明显的品牌+品类+幅度
2. 上新最多的品牌+品类占比
3. 品类扩展趋势判断
4. 下周重点关注建议

### 3.5 数据看板
Streamlit搭建，支持三个维度自助查询：
- 品牌分析：选品牌+天数 → 品类分布/价格变动/新品列表
- 品类对比：选品类+多店铺 → SKU数/均价/最高最低价
- 品类趋势：选品牌+品类+天数 → 价格变化折线图

---

## 四、技术难点与解决方案

### 难点1：海外平台WAF防护
**问题：** Naturehike、Cotopaxi等Shopify店铺的WAF拦截了Python requests请求，直接返回403或超时。

**排查过程：**
1. 先用浏览器确认URL能正常访问 → 确认是请求被拦截不是网站挂了
2. 添加完整浏览器请求头（User-Agent、Accept-Language等）→ 仍然被拦
3. 更换代理IP → 仍然不行
4. 对比浏览器和Python请求的差异 → 发现是TLS指纹不同

**解决方案：** 使用 `curl_cffi` 替代 `requests`。curl_cffi 能模拟Chrome浏览器的TLS指纹，让服务器认为是真实浏览器在访问。

**面试话术：** "我排查后发现不是IP或请求头的问题，是Python的requests库TLS握手特征跟浏览器不一样，被WAF识别为爬虫。换成curl_cffi模拟Chrome指纹后问题解决。"

### 难点2：eBay安全验证（Security Measure）
**问题：** Playwright访问eBay店铺页时触发机器人验证，返回"Security Measure"页面。

**排查过程：**
1. 用Playwright默认Chromium访问 → 被拦
2. 加SOCKS5代理 → 仍被拦
3. 加随机延迟、模拟真人操作 → 仍被拦
4. 改用系统已安装的Chrome（`channel='chrome'`）→ **通过验证**

**原因：** Playwright内置的Chromium没有缓存和Cookie，eBay识别为全新浏览器。系统Chrome有用户的登录态和Cookie，通过验证的概率更高。

**解决方案：** 使用 `channel='chrome'` 调用系统Chrome，访问前先打开Google建立合法会话。

**面试话术：** "eBay的Security Measure对无头浏览器很敏感。我发现用系统已安装的Chrome替代Playwright内置的Chromium后，通过率大幅提升。核心原理是系统Chrome有用户的历史行为数据，更像真人。"

### 难点3：企业级住宅代理配置
**问题：** 购买的9http住宅代理在Python中配置后始终连不上（ProxyError）。

**排查过程：**
1. 用curl命令测试代理 → 确认代理本身可用
2. 检查代理URL格式 → 发现用户名需要加地区后缀（`soxLrK8B-geo-US` 而不是 `soxLrK8B`）
3. 修正后仍然连不上 → 发现是环境网络限制，用户本地VPN与代理冲突

**最终方案：** 放弃代理，改用VPN提供的本地SOCKS5端口（`127.0.0.1:10808`）出口。

**面试话术：** "代理配置的核心坑是格式问题——用户名可能包含地区代码后缀，文档不一定写清楚。如果代理实在调不通，可以用本机VPN的SOCKS5端口作为替代出口。"

### 难点4：飞书/钉钉Webhook推送不稳定
**问题：** 飞书Webhook频繁报 `ConnectionResetError(10054)`，消息发不出去。

**排查过程：**
1. 单独测试Webhook URL → 正常返回200
2. 在main.py中调用 → 报错
3. 发现VPN开启时，国内API（飞书）被VPN路由到海外导致连接重置
4. 加上 `proxies={"http": None, "https": None}` 绕过VPN → 问题解决

**面试话术：** "公司网络环境复杂，VPN开启时国内API会被误路由。解决方案是在请求国内服务时显式禁用代理，通过 `proxies=None` 让请求走直连。"

### 难点5：MySQL中文编码问题
**问题：** 读取JSON文件时 `UnicodeDecodeError: 'gbk' codec can't decode byte`，写数据库时中文变乱码。

**原因：** Windows的 `open()` 默认使用GBK编码，而文件是UTF-8编码。

**解决方案：** 所有文件操作显式指定 `encoding='utf-8'`。

### 难点6：商品品类归类不准确
**问题：** Jackery的电源类商品全被标为"未分类"，因为标题中"Portable Power Station"没有匹配到中文关键词。

**解决方案：** 建立中英文双语的品类关键词映射表，覆盖品牌常用的标题关键词。

---

## 五、项目成果

### 5.1 数据规模
- 8家Shopify店铺 + 1家eBay店铺
- 日均采集约1600个商品
- MySQL累计10天+历史数据
- 品类覆盖：帐篷、户外电源、服装、攀岩装备、背包等20+品类

### 5.2 效率提升
- 原人工：每日2小时手动复制数据，每周4小时整理周报
- 自动化后：每日2分钟自动采集+分析+推送
- 节省95%人工时间

### 5.3 交付物
| 交付物 | 说明 |
|--------|------|
| `main.py` | 每日自动采集入口 |
| `dashboard.py` | 交互式数据看板 |
| `weekly_report.py` | AI周报自动生成 |
| `alert.py` | 实时告警系统 |
| `output/` | 每日Excel报表 |
| `logs/` | 每日运行日志 |
| MySQL | 历史数据存储 |

---

## 六、面试常见问题

### Q1：Shopify的数据是怎么拿到的？
A：Shopify提供了公开的JSON接口 `/collections/all/products.json`，直接GET请求就能拿到全部商品的标题、价格、品类等数据，不需要爬网页。

### Q2：遇到反爬了吗？怎么解决的？
A：遇到了。大牌Shopify店铺的WAF会拦截Python的requests请求。排查后发现是TLS指纹问题——Python和浏览器的TLS握手特征不同。用curl_cffi模拟Chrome指纹后解决。

### Q3：eBay的数据怎么拿的？
A：eBay防护更严，需要浏览器渲染。用Playwright调用系统已安装的Chrome来绕过安全验证，访问前先打开Google建立合法会话。同时eBay有旧版Finding API可用（免费），后续计划等生产环境API审批后切换到正式接口。

### Q4：数据量大了怎么办？会不会把目标网站压垮？
A：严格控制请求频率——每家店铺间隔2秒以上，失败了等5/10/20秒递增重试，3次仍失败就跳过。单店最多采集前200个SKU，不会对目标网站造成压力。

### Q5：这个项目你花了多长时间？
A：全流程从零搭建到v3.0稳定运行，约3周时间。其中约70%的时间花在解决反爬问题上，30%在数据存储和展示层。

### Q6：有没有用AI？
A：用了DeepSeek API自动生成周报——读MySQL中的本周数据，调用AI分析价格变动趋势、新品分布、品类变化，生成自然语言报告推送到飞书。

### Q7：如果目标网站改版了怎么办？
A：Shopify的JSON接口是官方标准接口，不会因前端改版而变动。如果Shopify关闭接口，备选方案是用Playwright模拟浏览器采集页面数据。

---

## 七、本地运行指南

### 安装依赖
```bash
pip install curl_cffi playwright mysql-connector-python streamlit pandas openpyxl requests
playwright install chromium
```

### 启动流程
```bash
# 1. 文件服务器（提供Excel下载）
python file_server.py

# 2. 数据采集（新开终端）
python main.py

# 3. 启动看板
streamlit run dashboard.py
```
